I have access to a comprehensive suite of tools for codebase interaction, system execution, and strategic orchestration. Here is the list of available tools:

### ≡ƒôé File System & Navigation
*   `list_directory`: Lists files and subdirectories in a specific path.
*   `read_file`: Reads the content of a file (supports targeted reading of line ranges).
*   `write_file`: Creates a new file or overwrites an existing one.
*   `replace`: Performs surgical, string-based replacements in a file (preferred for precise edits).
*   `glob`: Efficiently finds files matching specific patterns (e.g., `**/*.ts`).
*   `grep_search`: Searches for regular expression patterns within file contents.

### ≡ƒÆ╗ System & Shell
*   `run_shell_command`: Executes shell commands in the workspace (for builds, tests, git operations, etc.).

### ≡ƒöì Search & Web
*   `google_web_search`: Performs a grounded Google Search for up-to-date documentation and troubleshooting.

### ≡ƒºá Strategic Orchestration
*   `update_topic`: Manages narrative flow by starting new chapters or shifting strategic intent.
*   `enter_plan_mode`: Switches to a read-only mode for researching and designing complex changes.
*   `invoke_agent`: Delegates specific tasks to expert sub-agents (e.g., `codebase_investigator`, `generalist`).
*   `activate_skill`: Activates specialized expert guidance (e.g., `tdd-workflow`, `security-review`).

How can I help you with your workspace today?
